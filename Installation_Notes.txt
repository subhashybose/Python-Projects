Version - python -V

YouTube - pip install pytube (YouTube(<<URL>>))

QR Code - pip install qrcode (qrcode.make(<<URL>>))

tkinter - pip install tk (for GUI apps, built-in library)

icecream - pip install icecream (for better debug/logggin instead of print())